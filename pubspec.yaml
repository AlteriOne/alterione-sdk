name: alterione_sdk
description: A starting point for Dart libraries or applications.
version: 0.0.1
repository: https://github.com/AlteriOne/alterione-sdk
issue_tracker: https://github.com/AlteriOne/alterione-sdk/issues

environment:
  sdk: ^3.11.5

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
